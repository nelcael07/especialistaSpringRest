# depois do ? é as propriedades, nesse caso esta sendo usado uma para criar o banco se não existir
#serverTimezone é para configurar o fuso horario do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/restapi?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=sial

